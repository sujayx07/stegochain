{
  "name": "stegochain-blockchain",
  "version": "2.0.0",
  "description": "StegoChainV2 smart contract — Hardhat project with Ethereum Sepolia support",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deployV2.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deployV2.js --network sepolia",
    "verify:sepolia": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.6"
  }
}
